#pragma once

// This header previously included boost::interprocess for shared-memory catalog.
// Now using standard C++ types (single-process embedded architecture).

#include <unordered_map>
#include <functional>
#include <string>
#include <vector>
