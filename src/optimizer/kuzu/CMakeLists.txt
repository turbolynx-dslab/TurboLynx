file(GLOB_RECURSE KUZU_SRC_FILES "${CMAKE_CURRENT_SOURCE_DIR}/src/*.c*")

add_library_unity(
  s62gdb_kuzu
  OBJECT
  ${KUZU_SRC_FILES}
)

set(ALL_OBJECT_FILES
    ${ALL_OBJECT_FILES} $<TARGET_OBJECTS:s62gdb_kuzu>
    PARENT_SCOPE)