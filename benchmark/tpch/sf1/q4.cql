MATCH (li:LINEITEM)-[:IS_PART_OF]->(ord:ORDERS)
WHERE 
date(ord.O_ORDERDATE) >= date("1994-01-01") AND 
date(ord.O_ORDERDATE) < date("1994-04-01") AND
date(li.L_COMMITDATE) < date(li.L_RECEIPTDATE)
WITH distinct ord
RETURN ord.O_ORDERPRIORITY, COUNT(*) AS ORDER_COUNT
ORDER BY ord.O_ORDERPRIORITY;